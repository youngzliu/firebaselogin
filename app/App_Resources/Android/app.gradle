// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.firetest"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
